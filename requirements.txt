# Ядро: доменная модель и данные
pandas>=2.2
numpy>=1.26

# Парсинг PDF (Трек A / Трек B)
pdfplumber>=0.11
PyMuPDF>=1.24   # fitz — векторный слой, координаты, текст с позициями

# IFC (Трек B)
ifcopenshell>=0.7.0

# Тесты
pytest>=8.0
