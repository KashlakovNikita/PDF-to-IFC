# Задача 25: извлечение текста из data/raw/*.pdf

Отчёт по разведке: можно ли снять текст с листов, где он переведён в кривые, и
каким способом. Проверены два подхода — OCR по растру (pytesseract) и разбор
векторных контуров глифов с сопоставлением со шрифтом чертежа (PyMuPDF).
Парсер спецификации не писался, геометрия труб не трогалась.

Файл: `data/raw/Раздел РД №1_ТС_Парнас_Проект.pdf`, 38 страниц, 20 МБ.

Скрипты: `scripts/research_pdf_text.py` (OCR), `scripts/research_pdf_glyphs.py`
(вектор). Оба воспроизводят все числа ниже.

---

## Коротко

1. **Постановку нужно поправить в одном месте.** «Весь текст на листах плана и
   профиля переведён в кривые» — верно, но это 10 листов из 38. На 15 листах
   (стр. 15-29, в том числе **спецификации и ведомости**) текст лежит обычным
   текстовым слоем и берётся `page.get_text()` **без потерь и без OCR**. Я
   поначалу сам прочёл их как мусор — это была кодировка консоли, а не PDF.

2. **Из двух проверенных подходов выигрывает OCR**, и заметно. На листах с
   кривыми: 72.6% токенов на таблице спецификации, 92% на отметках профиля,
   50% на выносках узлов. Векторное сопоставление даёт 53.4% / 2% / 12.5%.

3. **Узкое место векторного подхода — не распознавание, а сегментация.** Буква
   на этих листах нарисована не одним контуром, а несколькими заливками-штрихами;
   собрать штрихи обратно в буквы и не склеить соседние — и есть вся задача.
   Там, где сегментация отработала, сопоставление точное: в строке
   `40х4,0 ГОСТ 103-76` из 16 выделенных групп 15 опознаны верно, а
   единственная ошибка — склейка «7» и «6» в одну группу. Расстояние
   совпадения у верных знаков 0.11-0.27, у склейки — 0.657.

4. **У векторного подхода есть жёсткий потолок, который не лечится кодом.** В
   ISOCPEUR Italic строчная «т» — это ровно контур латинской «m» (расстояние
   между ними **0.000**), «п» = «n» (0.000), «Т»=«T», «х»=«x», «М»=«M» — тоже
   0.000. Различить их по форме нельзя в принципе, нужен словарь. Пары «й»/«и»
   (0.142) и «ш»/«щ» (0.096) различаются слабее, чем типичный шум совпадения
   (~0.22), то есть тоже ненадёжны.

5. **Главная опасность OCR — не пропуски, а тихие подмены.** На профиле при 92%
   найденных отметок `28,83` прочиталось как `26,83`: правдоподобное число на
   правильном месте, никакого признака ошибки. Пропуск виден, подмена — нет.

6. **OCR теряет ровно то, что нужно парсеру.** На спецификации распознались
   длинные описания, а потеряны номера позиций (1-7), единицы (`м`, `м²`,
   `шт.`) и количества: `1,6` прочиталось как `16` (ошибка в 10 раз),
   `ГОСТ 28961-91` — как `ГОСТ 26961-91`. На плане потеряны `ДК13` и
   `Ф325х8,0/450-ППУ` — обозначение узла и марка трубы.

**Вывод для дорожной карты.** Ни один из подходов не даёт точности, при которой
с листов плана и профиля можно брать данные модели автоматически и без сверки.
Спецификации и ведомости (стр. 15-29) при этом доступны точно и бесплатно —
их и надо использовать как источник, а план и профиль оставить DXF/DWG, где
текст лежит объектами. Подробнее — в разделе «Что это значит для парсера».

---

## 1. Что в файле на самом деле

Разметка получена перебором страниц (`scripts/research_pdf_text.py`, и то же
делает уже существующий `scripts/inspect_pdf.py`):

| Страницы | Режим | Текст, символов | Картинок | Векторных путей |
|---|---|---|---|---|
| 1-3 | текстовый слой | 280-510 | 1 | 2-274 |
| 4 | кривые | 0 | 0 | 8242 |
| **5** | **растр** | 0 | 103 | 0 |
| 6-14 | кривые | 0 | 0-2 | 2567-38800 |
| 15-29 | текстовый слой | 968-2104 | 0-2 | 5-763 |
| 30-38 | растр (скан) | 0 | 2-6 | 0 |

Четыре режима, а не один:

- **стр. 15-29 — текст есть.** Это ведомости и спецификации. Извлекается
  полностью: `Труба Ст 325х8,0–ППУ1-Б–ПЭ-ОДК`, `ГОСТ 30732-2020`, `10,80`,
  `75,49`. OCR здесь не нужен вообще.
- **стр. 4, 6-14 — кривые.** Планы, профили, разрезы, узлы. Текста ноль, путей
  до 38 800. Это и есть предмет задачи.
- **стр. 5 — растр.** Это второй лист плана («План тепловой сети М1:500,
  лист 2.1»), собранный из 103 растровых вставок. Векторного слоя нет вовсе,
  поэтому подход 2 на нём неприменим по построению — только OCR.
- **стр. 30-38 — сканы.** Технические условия, наборный текст. Только OCR.

### Поправка про «мусор» в текстовом слое

Первый прогон показал на стр. 15-29 сплошные `\ufffd`, и это легко принять за
битую кодировку в PDF. Дело не в файле: кириллица ломалась при выводе в
консоль Windows (cp1251). С `PYTHONIOENCODING=utf-8` текст извлекается
корректным Unicode. Проверять такие вещи надо по кодам символов
(`page.get_text("rawdict")` → `ord(c)`), а не по виду в терминале: там коды
1058, 1057 — нормальные «Т» и «С».

---

## 2. Методика

### Фрагменты и эталон

Взято четыре фрагмента на листах с кривыми плюс один контрольный. Эталон по
каждому вычитан глазами с рендера 200-600 dpi (рендеры сохраняются скриптом,
неоднозначные места пересматривались с увеличением — например, стопка
`28,83 / 26,87 / 26,85` на профиле проверялась при 600 dpi отдельно).

| Ключ | Что это | Стр. | Токенов в эталоне | Чем труден |
|---|---|---|---|---|
| `f1_spec` | Спецификация на узел «А» | 11 | 73 | лучший случай: крупный горизонтальный текст на белом |
| `f2_profile` | Отметки продольного профиля | 9 | 50 | числа повёрнуты на 90°, строки плотные |
| `f3a_plan_labels` | Выноски узлов на плане (ТК-3) | 6 | 8 | мелкий текст, две разные высоты в одном фрагменте |
| `f3b_plan_dense` | Подписи существующих сетей | 6 | 9 | поворот ~40°, поверх цветных линий подосновы |
| `f4_scan` | Технические условия, скан | 30 | 60 | контроль: обычный наборный текст |

`f4_scan` добавлен намеренно. Без него низкие числа на чертежах нечем отличить
от криво настроенного OCR.

### Метрика

Считается **доля найденных эталонных токенов** (мультимножеством, без учёта
порядка). CER по склеенной строке здесь бессмыслен: порядок чтения на чертеже
произвольный, OCR обходит ячейки как хочет, и любое расхождение в обходе даёт
огромный CER при полностью верном тексте.

Точность даётся в двух режимах:

- **строгая** — посимвольное равенство;
- **мягкая** — с приведением кириллическо-латинских близнецов (`С`/`C`, `Т`/`T`,
  `х`/`x`, `М`/`M`, `о`/`o`…) и десятичного разделителя (`27.42` = `27,42`).

Мягкая честнее по отношению к обоим подходам: для парсера `ГOCT` с латинскими
буквами и `ГОСТ` — одно и то же. Ниже, если не сказано иное, приводится мягкая.

---

## 3. Подход 1: OCR (pytesseract)

### Настройка

Tesseract 5.4.0 в системе стоял, но:

- **его нет в PATH** — `pytesseract` падает с `TesseractNotFoundError`, путь
  задаётся явно (`C:\Program Files\Tesseract-OCR\tesseract.exe`);
- **русского языка в комплекте не было**, только `eng` и `osd`. Без `rus`
  мерить кириллический чертёж бессмысленно. `rus.traineddata` взят из
  `tessdata_best` и положен в `tmp/ocr/tessdata` (каталог передаётся через
  `--tessdata-dir`, системный не трогался).

Свип: DPI 100-600, `--psm` 3/4/6/11/12, для профиля дополнительно поворот листа
на 90°. Всего 139 прогонов, сырые выдачи сохранены в
`tmp/ocr/ocr_sweep*.json`.

### Результаты (лучшая конфигурация на фрагмент)

| Фрагмент | DPI | psm | Строгая | Мягкая | Точность | Найдено | Лишних |
|---|---|---|---|---|---|---|---|
| `f4_scan` (контроль) | 150 | 3 | 98.3% | **98.3%** | 88.1% | 59/60 | 8 |
| `f2_profile` | 150 | 12 | 92.0% | **92.0%** | 83.6% | 46/50 | 9 |
| `f1_spec` | 150 | 11 | 71.2% | **72.6%** | 89.8% | 53/73 | 6 |
| `f3a_plan_labels` | 150 | 6 | 37.5% | **50.0%** | 50.0% | 4/8 | 4 |
| `f3b_plan_dense` | 150 | 6 | 11.1% | **11.1%** | 9.1% | 1/9 | 10 |

Контрольный фрагмент даёт 98.3%. Значит, OCR настроен верно, и низкие числа на
плане — свойство чертежа, а не установки.

### Что выяснилось при настройке

**`--psm` важнее всего остального.** На таблице спецификации `psm 6`
(«однородный блок текста») даёт **0%**: разлинованная таблица ломает ему
сегментацию, на выходе `'U3M.\r\nAEE © re Е) НЕ\r\n'`. `psm 11` на том же
изображении — 72.6%. Разница между режимами больше, чем между всеми
остальными параметрами вместе.

**Больше DPI — хуже.** Оптимум почти везде 150 dpi, дальше падение:

| Фрагмент | 100 | 150 | 200 | 250 | 400 | 600 |
|---|---|---|---|---|---|---|
| `f1_spec` (psm 11) | 49.3% | **72.6%** | 65.8% | 64.4% | 41.1% | 32.9% |
| `f2_profile` (psm 12) | 10.0% | **92.0%** | 92.0% | 82.0% | 72.0% | — |

Это не парадокс: tesseract рассчитан на кегль около 30 px по высоте прописной.
Текст на этих листах уже крупный относительно вырезки, и при 600 dpi он
перерастает рабочий диапазон. Гнать чертёж на 600 dpi «для качества» —
проигрышная стратегия.

**Повёрнутый текст лучше отдавать `psm 12`, а не крутить лист.** Отметки
профиля подписаны вертикально. Поворот вырезки на 90° + `psm 11` даёт 76%.
Но `psm 12` (разреженный текст с определением ориентации) на **неповёрнутом**
листе даёт 92% — он сам разбирается с ориентацией каждого слова. Поворот всего
листа при этом ломает строки, идущие горизонтально.

### Где OCR ломается

**Тихая подмена цифры.** Лучший результат на профиле — 46 из 50 отметок. Но
среди «найденных» есть `26,83` там, где на чертеже `28,83`. Это не пропуск:
число правдоподобное, стоит в своей ячейке, никаким признаком не помечено.
Ещё в спецификации `ГОСТ 28961-91` → `ГОСТ 26961-91`. Для модели это хуже
пропуска: пропуск заметен, подмена уходит в данные молча.

**Теряется короткое, выживает длинное.** На спецификации не найдены:

```
1 | Полоса | м | 1,6 | 2 | м2 | 3 | м2 | 4 | Ст3 | перекрытия | м2 |
5 | 2 | 6 | 4 | 7 | 28961-91 | шт. | 4
```

То есть номера позиций 1-7, единицы измерения и количества — всё, что живёт в
узких ячейках по одному-двум знакам. А `Хомут стяжной`, `Плита перекрытия`,
`ГОСТ 19903-74` распознались. Для спецификации это ровно наоборот тому, что
нужно: описание можно взять из ведомости, а количество — нельзя.

**Десятичная запятая пропадает.** `1,6` → `16`, `0,8` → `08`, `1,90` → `190`.
Ошибка в 10-100 раз, и внешне результат выглядит как обычное число.

**Выноски узлов на плане.** При 50% найденного сырая выдача выглядит так:

```
TK-3 Граница проектирования AK13 7 T4 т? —/
```

`ТК-3` распозналось (латиницей), `Граница проектирования` — верно, номер
выноски `7` — верно. Но `ДК13` → `AK13`, `Т1,` → `T4`, `Т2` → `т?`,
`Ф325х8,0/450-ППУ` → `—/`. Обозначение узла и марка трубы — то, ради чего лист
и читают, — уничтожены. При другой конфигурации (400 dpi, psm 11) `ДК13`
превращается в `Van`, а марка трубы — в `yy`: вид мусора меняется, сам факт
потери — нет.

**Плотная зона плана — 11%, фактически ноль.** Один токен из девяти, десять
лишних. Сырая выдача: `Е & к © a2 wo ча 2 =! ВД a`. Подписи существующих
сетей (`ст.108`, `плм 150`, `2 ст.325`) повёрнуты на ~40° и лежат поверх
цветных линий подосновы; tesseract не вытягивает ничего.

---

## 4. Подход 2: векторные контуры глифов

### Идея и устройство

Кривые на этих листах — не произвольные: это обводка конкретного шрифта
чертежа. Значит, глифы можно не «узнавать» статистически, а **сопоставлять с
эталонными контурами**. Реализация — `scripts/research_pdf_glyphs.py`:

1. из content stream берутся залитые пути (`get_drawings()`, тип `f`) внутри
   фрагмента;
2. штрихи собираются в глифы (связные компоненты по пересечению bbox);
3. глифы группируются в строки по базовой линии, строки — в слова по зазору;
4. каждый глиф растрируется в нормированную сетку 40×40: масштаб от высоты
   прописной в строке, посадка от базовой линии (иначе «о» и «О» неразличимы);
5. эталоны — глифы ISOCPEUR Italic в той же сетке;
6. совпадение — XOR-расстояние, нормированное на объединение, с перебором
   сдвига ±2 px и ширинного коэффициента.

### Три вещи, которые пришлось выяснить по дороге

**Страницы повёрнуты.** У листов `rotation` 90 или 270, и `get_drawings()`
отдаёт координаты в **неповёрнутом** пространстве, а `page.rect` и
`get_pixmap(clip=...)` работают в повёрнутом. Прямая проверка вырезки по
координатам рендера даёт 0 путей. Нужен `page.derotation_matrix`.

**Буква — это несколько заливок, а не одна.** В залитых путях нет ни одной
кривой Безье: во фрагменте спецификации 20 033 элемента и все до одного —
отрезки. Каждая буква разложена на отдельные залитые штрихи: в среднем 2.16 на
глиф, распределение по 274 группам — 1 штрих у 82, 2 у 104, 3 у 55, 4 у 29,
5-6 у четырёх. Одна заливка ≠ один глиф, и это определяет всю сложность.

**Полный шрифт нашёлся в системе.** Подмножество ISOCPEUR, встроенное в сам
PDF на листах спецификаций, неполное: 102 глифа, **нет `Й`, `Х`, `Ъ`, `Ы`,
`Ю`, `Ж`**. С ним `Хомут` не мог распознаться в принципе. На машине стоит
AutoCAD, и в `C:\Windows\Fonts\isocpeui.ttf` лежит полный ISOCPEUR Italic —
603 глифа, вся кириллица. Замена подмножества на него — обязательный шаг.
Набор эталонов при этом пришлось сузить: в полном шрифте есть сербские и
македонские буквы (`Ј`, `Ѕ`, `Џ`, `Ћ`), неотличимые от латинских и
перехватывающие совпадения.

### Результаты

| Фрагмент | Лучшая по эталону | При автовыборе параметров | Найдено |
|---|---|---|---|
| `f1_spec` | **53.4%** | 39.7% | 39/73 |
| `f3a_plan_labels` | **12.5%** | 0.0% | 1/8 |
| `f2_profile` | **2.0%** | 2.0% | 1/50 |
| `f3b_plan_dense` | **0.0%** | 0.0% | 0/9 |
| `f4_scan` | — | — | путей нет, подход неприменим |

Два числа вместо одного: «лучшая по эталону» — верхняя граница метода, когда
ширинный коэффициент и ориентацию подобрали, подглядывая в ответ. «При
автовыборе» — когда их выбирают без эталона, по среднему расстоянию совпадения.
Для настоящего парсера значима вторая: эталона у него не будет.

### Почему это хуже OCR, хотя сопоставление точное

Само сопоставление формы работает. Разбор строки из спецификации (до того, как
был добавлен разрез слипшихся групп):

```
40х4,0ГОСТ103-76   ← эталон
40х4,0ГОСТ103-7    ← получено, 16 групп штрихов
```

Поглифовые расстояния: `4` — 0.239, `0` — 0.231, `х` — 0.250, `,` — 0.238,
`Г` — 0.208, `О` — 0.267, `С` — 0.211, `Т` — 0.214, `1` — 0.106, `3` — 0.173.
Пятнадцать групп из шестнадцати опознаны верно, расстояния лежат в узком
коридоре 0.11-0.27. Шестнадцатая — `w=8.16 pt` при обычных 4.2-5.4 и
расстоянии **0.657**: это склеенные в одну группу `7` и `6`. Ошибка не в
распознавании, а в том, что две буквы попали в одну группу штрихов. Заодно
видно, что само расстояние — рабочий признак склейки: 0.657 против 0.27 у
худшего из верных.

Отсюда вся дальнейшая работа шла в сегментацию, и каждый шаг давал прирост:

| Что сделано | `f1_spec`, мягкая |
|---|---|
| исходная версия | 13.7% |
| посадка эталона по краю краски, а не по перу (иначе `О`→`З`, `Т`→`Г`) | 38.4% |
| разрез слипшихся групп штрихов | 52.1% |
| полный шрифт вместо встроенного подмножества | 46.6% |
| разрез по результату + подбор допуска строк | 53.4% |

Полный шрифт в этой таблице выглядит шагом назад, и по числу так и есть: 52.1%
→ 46.6% (строгая — 49.3% → 35.6%). Причина в том, что вместе с недостающими
`Х`, `Й`, `Ы`, `Ж` в набор эталонов пришла латиница, и на однобуквенных
совпадениях `С`/`C`, `Т`/`T` начала выигрывать она. Оставить подмножество всё
равно нельзя: без него `Хомут`, `Гайка`, `перекрытия` не могут быть распознаны
в принципе, — а латинские двойники снимаются нормализацией на выходе.

Характерно, что решение «резать группы шире обычного» сначала сломало честно
широкие буквы (`К` превращалась в `IQ`, `Ш` — в `Ш`+`I`). Рабочим оказался
разрез по результату: группа режется только если части опознаются увереннее
целого. И симметрично — группировка строк по вертикальному перекрытию bbox,
которая напрашивается первой и правильно ловит нижние выносы у «р», «у», «щ»,
здесь оказалась хуже (31.5% против 53.4%): в спецификации есть дроби, у
которых строки числителя и знаменателя перекрываются и сливаются в одну.

### Потолок метода

Помимо сегментации есть предел, который кодом не убирается. Расстояния между
эталонами в самом ISOCPEUR Italic:

| Пара | Расстояние | |
|---|---|---|
| `т` / `m` | **0.000** | контуры совпадают побайтово |
| `п` / `n` | **0.000** | |
| `Т` / `T`, `х` / `x`, `М` / `M` | **0.000** | |
| `С` / `C` | 0.010 | |
| `ш` / `щ` | 0.096 | ниже шума совпадения |
| `й` / `и` | 0.142 | ниже шума совпадения |
| `а` / `о` | 0.200 | на уровне шума |

Типичное расстояние **верного** совпадения — 0.21-0.27. То есть `й` от `и` и
`ш` от `щ` отличаются меньше, чем верный ответ отличается от самого себя из-за
растеризации. Это не недоработка: в наклонном начертании кириллическая «т»
рисуется как «m», и различить их по форме нельзя никакими средствами. Нужен
словарь или языковая модель — ровно то, что уже встроено в tesseract и чего
нет в голом сопоставлении контуров.

### Где подход неприменим по построению

- **`f2_profile` — 2%.** Отметки подписаны вертикально. Доворот фрагмента
  реализован, но не спасает: в плотной таблице профиля соседние повёрнутые
  числа склеиваются в общие группы штрихов ещё до распознавания.
- **`f3b_plan_dense` — 0%, и это структурно.** Во фрагменте 38 заливок против
  57 штриховых путей, медианный размер заливки 0.96 pt — глифоподобных всего
  6 на ~25 ожидаемых знаков. Подписи подосновы (`ст.108`, `плм 150`) нарисованы
  **штрихами, а не залитыми контурами**: это топосъёмка, пришедшая в чертёж
  из другого источника. Метод, который ищет заливки, там искать нечего.
  Полезное следствие: проектный текст (`ТК-3`, спецификации, отметки) — заливки
  и подходу 2 доступен; текст подосновы — нет.
- **`f3a_plan_labels` — 12.5%.** Здесь мешает смесь размеров: `ТК-3` и `ДК13`
  набраны крупнее остального и слипаются целиком — `ТК-3` даёт одну группу
  шириной 27.24 pt при высоте 14.16. Оценка высоты прописной по такой группе
  завышена (в неё попадает начало выноски), нормировка уезжает, и разрез уже
  не помогает.
- **`f4_scan`** — векторного слоя нет вовсе, подход неприменим.

---

## 5. Что это значит для парсера

**Спецификации и ведомости брать из текстового слоя (стр. 15-29), а не OCR'ом.**
Это единственный источник в файле, дающий 100% без оговорок. Обязательно
`PYTHONIOENCODING=utf-8` или явная работа с `rawdict`.

**С листов плана и профиля автоматически данные не брать.** Числа лучшего
прогона выглядят прилично (92% отметок), но:

- тихие подмены цифр (`28,83`→`26,83`) не отлавливаются ничем, кроме сверки;
- теряются именно ключевые поля — обозначения узлов, марки труб, количества;
- десятичная запятая пропадает, давая ошибку в 10-100 раз.

Для модели, где отметка лотка и диаметр идут в геометрию, такая точность
означает ручную проверку каждого числа. Это не дешевле, чем ввести их руками
(`manual_input`), и заметно опаснее, потому что создаёт видимость автоматизации.

**Если источник данных всё же нужен — это DXF/DWG, а не PDF.** В
`data/reference/dwg` лежат исходники заказчика, где текст — объекты с
координатами и без всякого распознавания. Судя по истории (задача 33,
`scripts/extract_from_dwg.py`), этот путь в проекте уже размечен. Разведка
подтверждает: PDF как источник текста для плана и профиля — тупиковая ветка,
и вкладываться надо в DXF.

**Если OCR всё-таки применяется** (например, для стр. 5 и 30-38, где другого
слоя нет), то по итогам свипа: 150 dpi, `--psm 11` для таблиц и `--psm 12` для
листов с повёрнутыми подписями, `-l rus+eng`, с обязательной установкой
`rus.traineddata` — и с ручной сверкой каждого числа, уходящего в модель.

---

## 6. Что осталось непроверенным

Чтобы не выдавать пределы этой разведки за пределы самих подходов:

- **Предобработка растра не проверялась** — бинаризация, убирание цветных
  линий подосновы по каналам, morphological opening. На `f3b`, где мешают
  цветные линии, это может что-то дать. `cv2` в окружении не стоит.
- **Дообучение tesseract на ISOCPEUR не пробовалось.** Шрифт известен и
  доступен, синтетический корпус делается из него же — это самый очевидный
  следующий шаг, если OCR решат тянуть дальше. Ожидаемо снимет и часть
  латинско-кириллических подмен.
- **Гибрид не пробовался**: сегментация по векторным путям (границы глифов
  известны точно) + распознавание вырезанного растра tesseract'ом. Это обходит
  главную слабость обоих подходов — у подхода 2 слабая сегментация при точном
  сопоставлении, у OCR наоборот.
- **Другие OCR-движки** (PaddleOCR, EasyOCR, облачные) не проверялись.
- **Сегментация подхода 2 доведена не до предела**: разбор повёрнутого текста
  и смеси размеров в одном фрагменте остались нерешёнными, 2% и 12.5% — это
  оценка реализации, а не потолка идеи. Потолок из раздела 4 (неразличимые
  `т`/`m`) при этом никуда не денется.

---

## 7. Воспроизведение

```bash
# OCR: полный свип по фрагментам, DPI и psm
python scripts/research_pdf_text.py ocr --dpi 100 150 200 250 400 \
    --psm 11 12 4 3 --json tmp/ocr/ocr_sweep.json

# только вырезать фрагменты в tmp/ocr
python scripts/research_pdf_text.py render --dpi 300

# вектор: разбор всех фрагментов
python scripts/research_pdf_glyphs.py --all --json tmp/ocr/glyph_result.json
python scripts/research_pdf_glyphs.py --frag f1_spec --show
```

Требуется, помимо `requirements.txt`:

- **tesseract 5.x**; в PATH его нет, путь зашит в
  `research_pdf_text.TESSERACT_EXE`;
- **`rus.traineddata`** в `tmp/ocr/tessdata` (в комплекте tesseract на этой
  машине не было). Берётся из `tessdata_best`;
- **ISOCPEUR Italic**: `C:\Windows\Fonts\isocpeui.ttf` (ставится с AutoCAD).
  Если его нет, скрипт откатывается на неполное подмножество, извлечённое из
  самого PDF, — с заметно худшим результатом;
- **`PYTHONIOENCODING=utf-8`** — иначе кириллица в выводе превращается в
  `\ufffd`, что легко принять за проблему в PDF.

Вырезки фрагментов, сырые выдачи OCR и JSON с результатами пишутся в `tmp/` и
в гит не идут; все числа отчёта пересобираются командами выше.
